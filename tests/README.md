# NexusOS Test Suite (`tests/`)

This directory contains repository-wide end-to-end, contract, security, and sanity verification tests.

---

## 🧪 Running Tests

Run the test suite using standard root commands:

```bash
npm run test
```

- [`sanity.test.ts`](file:///c:/Users/priya/Desktop/Nexus%20AI/tests/sanity.test.ts): Verifies monorepo environment, path resolution, and basic contract package imports.
