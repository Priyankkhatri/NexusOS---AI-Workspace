# NexusOS Infrastructure (`infrastructure/`)

This directory houses infrastructure configuration, database migration frameworks, and local development infrastructure configurations.

---

## 🏗️ Structure (Phase 1+)

- `docker/`: Docker Compose manifests for local PostgreSQL, Redis, and Event Bus setup.
- `migrations/`: SQL migration scripts and schema definitions.
- `terraform/`: Cloud infrastructure definitions.
